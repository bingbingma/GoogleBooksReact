import React from "react";
import ReactDOM from "react-dom";
import "bootstrap/dist/css/bootstrap.css";
import App from "./App";

const element = <h1>Hello Wsadforld</h1>;

console.log(element);

ReactDOM.render(<App />, document.getElementById("root"));

// React.createElement;
//Navbar increments how many items you have nonzero quantities for
//reset
//quantity in a cart, button changes color, add subtract buttons, delete button to remove
//add an item? not shown in the demo
//
